/* ============================================================
   CONFIGURACIÓN  ·  edita SOLO este archivo
   ============================================================
   1) Crea una Google Sheet y un Google Apps Script (ver
      servidor/seguimiento.gs y el README).
   2) Despliega el script como "Aplicación web" (acceso: cualquiera).
   3) Copia la URL que termina en /exec y pégala abajo.

   Si lo dejas vacío (""), el sitio FUNCIONA igual: guarda todo
   localmente en el navegador (modo demo) y el dashboard lo lee
   desde ahí. Ideal para probar antes de publicar.
============================================================ */

window.CONFIG = {
  // Pega aquí tu URL de Apps Script (termina en /exec):
  ENDPOINT: "",

  // Nombre del curso (aparece en la barra y el dashboard)
  CURSO: "Arquitectura de Servidores",
  UNIDAD: "Unidad 1 · Organización computacional",

  // Clave simple para abrir el dashboard de la docente
  DASHBOARD_PIN: "1234"
};
